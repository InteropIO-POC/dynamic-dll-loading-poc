<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Margin="20" VerticalAlignment="Top">
        <TextBlock Text="Status:" FontWeight="Bold"/>
        <TextBlock x:Name="StatusText" Text="Connecting..." Margin="0,4,0,12"/>
        <TextBlock Text="Runtime Info:" FontWeight="Bold"/>
        <TextBlock x:Name="PlatformText" Margin="0,4,0,0"/>
        <TextBlock x:Name="VersionText" Margin="0,4,0,20"/>
        <TextBlock Text="FDC3 Open:" FontWeight="Bold"/>
        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
            <Grid Width="200" Margin="0,0,8,0">
                <TextBox x:Name="AppNameInput" Padding="2,0,0,0"/>
                <TextBlock Text="App name..." Foreground="Gray" Margin="4,0,0,0"
                           VerticalAlignment="Center" IsHitTestVisible="False">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Text, ElementName=AppNameInput}" Value="">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
            <Button x:Name="OpenButton" Content="Open" IsEnabled="False" Click="OpenButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
